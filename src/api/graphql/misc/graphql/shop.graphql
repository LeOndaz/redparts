query getCountries ($languageCode: LanguageCodeEnum!){
    shop {
        countries(languageCode: $languageCode){
            country
            code
        }
    }
}

#query testError {
#  user(id: "VXNlcjox" ){
#    id
#  }
#}
